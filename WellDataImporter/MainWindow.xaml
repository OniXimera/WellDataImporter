<Window x:Class="WellDataImporter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WellDataImporter"
        xmlns:resx="clr-namespace:WellDataImporter.Resources;assembly=WellDataImporter"
        xmlns:viewModels="clr-namespace:WellDataImporter.ViewModels"
        mc:Ignorable="d"
        Title="{x:Static resx:Resources.AppTitle}" 
        Height="600" 
        Width="900">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Button Content="{x:Static resx:Resources.BtnImport}" Command="{Binding ImportFileCommand}" Width="120" Height="30" Margin="0,0,10,0"/>
            <Button Content="{x:Static resx:Resources.BtnExport}" Command="{Binding ExportJsonCommand}" Width="120" Height="30"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Summaries}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{x:Static resx:Resources.ColWellId}" Binding="{Binding WellId}" Width="*"/>
                <DataGridTextColumn Header="{x:Static resx:Resources.ColDepth}" Binding="{Binding TotalDepth}" Width="*"/>
                <DataGridTextColumn Header="{x:Static resx:Resources.ColIntervals}" Binding="{Binding IntervalCount}" Width="*"/>
                <DataGridTextColumn Header="{x:Static resx:Resources.ColPorosity}" Binding="{Binding AveragePorosity}" Width="*"/>
                <DataGridTextColumn Header="{x:Static resx:Resources.ColRock}" Binding="{Binding DominantRock}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="{x:Static resx:Resources.LblErrorList}" FontWeight="Bold" Margin="0,10,0,5"/>

        <DataGrid Grid.Row="3" ItemsSource="{Binding Errors}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Foreground="Red">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{x:Static resx:Resources.ColLine}" Binding="{Binding LineNumber}" Width="100"/>
                <DataGridTextColumn Header="{x:Static resx:Resources.ColWellId}" Binding="{Binding WellId}" Width="120"/>
                <DataGridTextColumn Header="{x:Static resx:Resources.ColError}" Binding="{Binding ErrorMessage}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="4" Margin="-10,10,-10,-10">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar IsIndeterminate="{Binding IsBusy}" Width="100" Height="15" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
